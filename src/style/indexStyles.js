import { createUseStyles } from "react-jss"

export default createUseStyles({
    bigContainer: {
        maxWidth: "1024px",
        margin: "0 auto",
    },
    marginContainer: {
        margin: "0 auto",
        width: "90%",
    },
})
