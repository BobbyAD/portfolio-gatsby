import { createUseStyles } from "react-jss"

export default createUseStyles({
    container: {
        width: "100%",
        display: 'flex',
        flexDirection: 'column',
    },
    title: {
        fontSize: "1.5rem",
        fontWeight: "600",
    }
})
