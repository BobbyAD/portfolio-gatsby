// display current and next, hide next
// animate images moving left <--
// current = next
// instantly swap to current being displayed, no animation
// next = img[next+1] || img[0];
// repeat
// pause if hovered?
// add arrows?